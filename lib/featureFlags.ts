// Flip this to true once Razorpay is wired up and Pro is actually purchasable.
// Until then, gating free users out of downloads for a plan they can't buy would
// just be a dead end — so everything stays unlocked regardless of plan.
export const PAYWALL_ENABLED = false;
